/**
 * add tags name description here for a particular module.
 * the tag name is used to match the module docs to the overall document.
 */

export default {
  tags: [
    {
      name: 'auth',
      description: 'Auth Operations'
    }
  ]
};
